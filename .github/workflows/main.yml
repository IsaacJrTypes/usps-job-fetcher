name: Run Scraper and Send to n8n

on:
  schedule:
    - cron: '0 20 * * *'  # Run every day at 12 PM PST (8 PM UTC)
  workflow_dispatch:      # Allow manual trigger from GitHub UI
  push:
    branches:
      - main              # Run on push to main (optional)

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm run ci

      - name: Debug environment
        run: |
          echo "Webhook URL set: ${{ vars.N8N_WEBHOOK_URL != '' }}"
          echo "Webhook URL: ${N8N_WEBHOOK_URL:0:20}..."
      
      - name: Run scraper
        run: node index.js
        env:
          N8N_WEBHOOK_URL: ${{ vars.N8N_WEBHOOK_URL }}
          
      # - name: Upload results (optional)
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: scraper-results
      #     path: results.json
      #     retention-days: 30